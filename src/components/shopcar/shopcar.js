import { Component } from 'react';
import './shopcar.less'

export default class shopcar extends Component {

    constructor(props) {
        super(props)
    }

    render() {
        return (
            <div className="shopcar">shopcar</div>
        )
    }
    
}